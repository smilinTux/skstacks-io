# skstacks.io — this domain now redirects to the primary site:
#   https://skstacks.skworld.io/
# Indexing/SEO is handled there. This page is noindex,follow so the
# redirect is still followed; crawling the redirect itself is allowed.

User-agent: *
Allow: /
